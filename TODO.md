# TODO

- context-mode's runners on an attached environment: `ctx_execute`,.
  `ctx_execute_file` and `ctx_batch_execute` run on the laptop and are refused
  while `/environment` has a machine attached.
  three routes, costs, and what their compiled output actually does:
  [docs/context-mode-remote-TODO.md](docs/context-mode-remote-TODO.md).
- `/remote connect` has never rendered an answer from a real model, only the.
  plumbing around it: [docs/remote-testing.md](docs/remote-testing.md).
